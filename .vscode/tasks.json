{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "CMake build",
            "command": "cmake",
            "args": ["--build", "."],
            "options": {
                "cwd": "${workspaceFolder}\\build"
            },
            "problemMatcher": ["$gcc"],
            "group": "build"
        }
    ]
}

